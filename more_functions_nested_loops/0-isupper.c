#include <stdio.h>
#include "main.h"
/**
 * print_alphabet_x10 -   prints the alphabet, in lowercase
 * Return: Always (Success)
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else

		return (0);
}