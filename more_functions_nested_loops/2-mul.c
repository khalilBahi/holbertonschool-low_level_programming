#include <stdio.h>
#include "main.h"
/**
 * mul -  function that multiplies two integers
 * Return: Always (Success)
 * @a : integer
 * @b : integer
 */
int mul(int a, int b)
{
	int s = 0;

	s = a * b;
	return (s);
}
