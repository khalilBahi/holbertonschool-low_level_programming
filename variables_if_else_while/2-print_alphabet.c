#include <stdio.h>
/**
 * main -  print the the alphabet in lowercase
 *
 * Return: Always (Success)
 */

int main(void)
{

	char ch = 'a';

	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);

	return (0);
}
