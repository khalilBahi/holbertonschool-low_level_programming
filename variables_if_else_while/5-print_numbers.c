#include <stdio.h>
/**
 * main - Prints the alphabet in lowercase
 *
 * Return: Always (Success)
 */
int main(void)
{
	int a;

	for (a = '0'; ch <= '10'; a++)
	{
		printf("%d\n", a);
	}
	return (0);
}
