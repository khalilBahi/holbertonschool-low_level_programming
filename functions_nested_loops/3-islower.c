#include <stdio.h>
#include "main.h"
/**
 * _islower -   prints the alphabet, in lowercase
 * Return: Always (Success)
 * @c: integer
 */
int _islower(int c)
{
	if (c >= 97)
	{
		return (1);
	}
	else

		return (0);
}
