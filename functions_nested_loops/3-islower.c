#include <stdio.h>
#include "main.h"
/**
 * _islower -   prints the alphabet, in lowercase
 * Return: Always (Success)
 */
int _islower(int c)
{
	if (c >= 97)
	{
		return (1)
	}
	else

		return (0)
}
