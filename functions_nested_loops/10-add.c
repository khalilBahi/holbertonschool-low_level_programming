#include "main.h"
/**
 * add - function that adds two integers and returns the result
 * @a: integer
 * @b: integer
 * s: integer
 * Return: Always (Success)
 */

int add(int a, int b)
{
	int s = 0;

	s = a + b;
	return (s);
}
